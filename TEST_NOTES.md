# Test artifact

This branch exists to exercise AsOf's D1 (Done-but-not-merged) rule against real Jira +
GitHub data: KAN-1 is marked Done on the board, but this PR (which references it) is
intentionally left open and unmerged.

Safe to delete once the D1 test has been observed.
